name: Deploy Foodgram

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Build and push backend image
      uses: docker/build-push-action@v4
      with:
        context: ./backend
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/foodgram_backend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Build frontend
      run: |
        cd frontend
        npm ci --legacy-peer-deps
        GENERATE_SOURCEMAP=false CI=false npm run build
    
    - name: Build and push frontend image
      uses: docker/build-push-action@v4
      with:
        context: ./frontend
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/foodgram_frontend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    
    - name: Deploy to server
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          # Установка Docker и Docker Compose если не установлены
          if ! command -v docker &> /dev/null; then
            echo "Устанавливаем Docker..."
            curl -fsSL https://get.docker.com -o get-docker.sh
            sudo sh get-docker.sh
            sudo usermod -aG docker $USER
            rm get-docker.sh
          fi
          
          if ! command -v docker-compose &> /dev/null; then
            echo "Устанавливаем Docker Compose..."
            sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
          fi
          
          # Создаем директории проекта
          PROJECT_DIR="/home/${{ secrets.USERNAME }}/foodgram"
          
          # Удаляем старую директорию если есть и создаем новую
          sudo rm -rf $PROJECT_DIR
          mkdir -p $PROJECT_DIR
          cd $PROJECT_DIR
          
          # Клонируем репозиторий
          git clone https://github.com/${{ github.repository }}.git .
          
          # Делаем пользователя владельцем директории
          sudo chown -R ${{ secrets.USERNAME }}:${{ secrets.USERNAME }} $PROJECT_DIR
          
          cd infra
          
          # Создаем .env файл с секретами
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" > .env
          echo "DEBUG=False" >> .env
          echo "ALLOWED_HOSTS=foodgrammm.3utilities.com,www.foodgrammm.3utilities.com,${{ secrets.DOMAIN_NAME }},www.${{ secrets.DOMAIN_NAME }},localhost,127.0.0.1,51.250.29.108" >> .env
          echo "DB_ENGINE=django.db.backends.postgresql" >> .env
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> .env
          echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" >> .env
          echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> .env
          echo "DB_HOST=db" >> .env
          echo "DB_PORT=5432" >> .env
          echo "DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}" >> .env
          echo "DOMAIN_NAME=${{ secrets.DOMAIN_NAME }}" >> .env
          
          # Всегда используем HTTP конфигурацию (SSL настраивается вручную)
          echo "🌐 Используем HTTP конфигурацию"
          sed -i 's/nginx.prod.conf/nginx.simple.conf/' docker-compose.prod.yml
          
          # Обновляем nginx конфигурацию с доменом
          sed -i 's/your-domain.com/${{ secrets.DOMAIN_NAME }}/g' nginx.simple.conf
          
          # Запускаем контейнеры
          docker-compose -f docker-compose.prod.yml pull
          docker-compose -f docker-compose.prod.yml up -d --build
          
          # Ждем запуска базы данных
          sleep 30
          
          # Принудительно перезагружаем backend для обновления кода
          echo "🔄 Перезагружаем backend контейнер для применения изменений..."
          docker-compose -f docker-compose.prod.yml restart backend
          
          # Ждем полного запуска backend
          echo "⏳ Ждем запуска backend..."
          sleep 10
          
          # Выполняем миграции и инициализацию
          docker-compose -f docker-compose.prod.yml exec -T backend python manage.py migrate
          docker-compose -f docker-compose.prod.yml exec -T backend python manage.py collectstatic --noinput
          
          # Загружаем данные только если их нет
          docker-compose -f docker-compose.prod.yml exec -T backend python manage.py shell -c "
          from recipes.models import Tag, Ingredient
          if not Tag.objects.exists():
              import subprocess
              subprocess.run(['python', 'manage.py', 'load_tags_data'])
              print('Теги загружены')
          else:
              print('Теги уже существуют')
          if not Ingredient.objects.exists():
              import subprocess  
              subprocess.run(['python', 'manage.py', 'load_ingredients_data'])
              print('Ингредиенты загружены')
          else:
              print('Ингредиенты уже существуют')
          "
          
          # Создаем суперпользователя
          docker-compose -f docker-compose.prod.yml exec -T backend python manage.py shell -c "
          from django.contrib.auth import get_user_model
          User = get_user_model()
          if not User.objects.filter(email='admin@foodgram.com').exists():
              User.objects.create_superuser('admin', 'admin@foodgram.com', 'admin123')
              print('Суперпользователь создан: admin@foodgram.com / admin123')
          else:
              print('Суперпользователь уже существует')
          "
          
          # Очищаем старые образы
          docker system prune -f
          
          # Проверяем статус контейнеров
          docker-compose -f docker-compose.prod.yml ps
          
          echo "✅ Деплой завершен успешно!"
          echo "🌐 Сайт доступен по адресу: http://foodgrammm.3utilities.com"
          echo "👤 Админка: http://foodgrammm.3utilities.com/admin/"
          echo "📚 API документация: http://foodgrammm.3utilities.com/api/docs/"
          echo ""
          echo "👤 Данные администратора:"
          echo "   Логин: admin@foodgram.com"
          echo "   Пароль: admin123"
          echo ""
          echo "🔒 Для настройки SSL вручную:"
          echo "   1. Получите сертификат: sudo certbot --nginx -d foodgrammm.3utilities.com"
          echo "   2. Переключите на HTTPS: sed -i 's/nginx.simple.conf/nginx.prod.conf/' docker-compose.prod.yml"
          echo "   3. Перезапустите nginx: docker-compose restart nginx"
